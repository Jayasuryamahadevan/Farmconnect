version: '3.8'

services:
  farmconnect:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: farmconnect_app
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
